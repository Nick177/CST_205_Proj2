from PIL import Image
import convert


#Uncomment multiline comment below when there is a image in folder
#type picture name in open() function below
"""

im = image.open(file name goes here)

width, height = im.size

rgb_im = im.convert('RGB')
"""

msg = "hi"

msgLength = len(msg)

#below is a unfinished for loop that might be needed
for ix in range(0, msgLength):
	asciNum = convertToAsci(msg[ix])
	letterBin = convertToBinary(asciNum)
	letterLength = len(letterBin)
	
	for jx in range(0, letterLength):
		


#r, g, b = rgb_im.getPixel((x, y))









